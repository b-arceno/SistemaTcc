<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Meu Carrinho</title>
  <link rel="stylesheet" href="/css/style.css" />
  <style>
    .carrinho-container {
      max-width: 900px;
      margin: 60px auto;
      background: #fff0f6;
      padding: 40px;
      border-radius: 25px;
      box-shadow: 0 8px 25px rgba(233,158,185,0.5);
    }

    h1 {
      text-align: center;
      color: #d9155c;
      font-size: 28px;
      margin-bottom: 25px;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 25px;
      background: #fffafc;
      border-radius: 15px;
      overflow: hidden;
      box-shadow: 0 4px 12px rgba(233,158,185,0.3);
    }

    th, td {
      padding: 14px 12px;
      text-align: left;
    }

    th {
      background-color: #f48bae; 
      color: #700a32; 
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    td {
      background-color: #fff0f6;
    }

    tr:nth-child(even) td { background-color: #ffe6f0; }
    tr:hover td { background-color: #fddde9; transition: 0.2s; }

    td img {
      width: 70px;
      height: 70px;
      object-fit: cover;
      border-radius: 10px;
      box-shadow: 0 3px 6px rgba(178,62,178,0.15);
    }

    .quantidade-input {
      width: 50px;
      text-align: center;
      border: 1px solid #e1c4e9;
      border-radius: 10px;
      padding: 4px;
      font-weight: 600;
      background: #fff;
    }

    .btn-remover {
      background: linear-gradient(135deg,#ff6b6b,#f87171);
      color: #fff;
      border: none;
      border-radius: 15px;
      padding: 6px 12px;
      font-weight: 600;
      cursor: pointer;
      transition: 0.3s;
    }
    .btn-remover:hover {
      background: linear-gradient(135deg,#ff8585,#fca5a5);
    }

    .total-carrinho {
      text-align: right;
      margin-top: 25px;
      font-size: 1.3rem;
      color: #d9155c;
      font-weight: 700;
    }

    .btn-continuar, .btn-finalizar {
      background: linear-gradient(135deg,#d9155c,#f48bae);
      color: #fff;
      border: none;
      border-radius: 25px;
      padding: 12px 22px;
      text-decoration: none;
      font-weight: 700;
      transition: 0.3s ease;
    }
    .btn-continuar:hover, .btn-finalizar:hover {
      background: linear-gradient(135deg,#f48bae,#ffe6f0);
      color: #700a32;
    }

    .acoes-carrinho {
      display: flex;
      justify-content: space-between;
      margin-top: 30px;
    }

  </style>
</head>
<body>
  <%- include('../partials/header') %>

  <main class="carrinho-container">
    <h1>Meu Carrinho</h1>

    <% if (carrinho.length > 0) { %>
      <table>
        <thead>
          <tr>
            <th>Produto</th>
            <th>Quantidade</th>
            <th>Preço</th>
            <th>Subtotal</th>
            <th>Remover</th>
          </tr>
        </thead>
        <tbody>
          <% carrinho.forEach(item => { %>
            <tr>
              <td><%= item.nome %></td>
              <td><%= item.quantidade %></td>
              <td>R$ <%= Number(item.preco).toFixed(2) %></td>
              <td>R$ <%= (Number(item.preco) * item.quantidade).toFixed(2) %></td>
              <td>
                <form action="/loja/carrinho/remover/<%= item.id %>" method="POST">
                  <button type="submit" class="btn-remover">❌</button>
                </form>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>

      <p class="total-carrinho">Total: R$ <%= Number(total).toFixed(2) %></p>

      <div class="acoes-carrinho">
        <a href="/loja" class="btn-continuar">⬅ Continuar Comprando</a>
        <form action="/loja/checkout" method="GET">
          <button type="submit" class="btn-finalizar">Finalizar Pedido ➜</button>
        </form>
      </div>

    <% } else { %>
      <p style="text-align:center; color:#700a32; font-weight:600; margin-top:40px;">
        Seu carrinho está vazio. <a href="/loja">Voltar para a loja</a>
      </p>
    <% } %>
  </main>

  <%- include('../partials/footer') %>
</body>
</html>
