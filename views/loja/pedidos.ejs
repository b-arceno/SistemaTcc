<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Meus Pedidos</title>
  <link rel="stylesheet" href="/css/style.css" />
  <style>
    .pedidos-container {
      max-width: 1000px;
      margin: 60px auto;
      background: #fff0f6; /* rosa claro */
      border-radius: 25px;
      padding: 40px;
      text-align: center;
      box-shadow: 0 8px 25px rgba(233,158,185,0.5);
    }

    h1 {
      color: #d9155c;
      font-size: 2rem;
      font-weight: bold;
      margin-bottom: 30px;
      text-align: center;
    }

    .pedidos-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 25px;
    }

    .pedido-card {
      background: #fffafc;
      border-radius: 25px;
      padding: 20px;
      text-align: left;
      box-shadow: 0 4px 12px rgba(233,158,185,0.3);
      transition: 0.3s ease;
      position: relative;
    }

    .pedido-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 6px 20px rgba(233,158,185,0.4);
    }

    .pedido-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 12px;
    }

    .pedido-header h3 { 
      color: #d9155c; 
      font-weight: 600; 
      font-size: 1.1rem;
    }

    .pedido-header span { 
      font-size: 0.95rem; 
      color: #700a32; 
    }

    .pedido-status {
      display: inline-block;
      margin-top: 10px;
      padding: 6px 12px;
      border-radius: 12px;
      font-weight: 600;
      text-transform: capitalize;
      font-size: 0.9rem;
    }

    .pedido-status.pendente { background: #fff3cd; color: #b45309; }
    .pedido-status.em\ preparo { background: #fde68a; color: #92400e; }
    .pedido-status.finalizado { background: #bae6fd; color: #0369a1; }
    .pedido-status.a\ caminho { background: #ddd6fe; color: #5b21b6; }
    .pedido-status.entregue { background: #bbf7d0; color: #166534; }
    .pedido-status.cancelado { background: #fecaca; color: #991b1b; }

    .pedido-total {
      margin-top: 15px;
      font-weight: 700;
      text-align: right;
      color: #d9155c;
    }

    .btn-detalhes {
      display: inline-block;
      margin-top: 15px;
      background: linear-gradient(135deg, #d9155c, #f48bae);
      color: #fff;
      font-weight: 600;
      border: none;
      border-radius: 25px;
      padding: 10px 18px;
      cursor: pointer;
      text-decoration: none;
      text-align: center;
      transition: 0.3s ease;
    }

    .btn-detalhes:hover {
      background: linear-gradient(135deg, #f48bae, #ffe6f0);
      color: #700a32;
      transform: translateY(-2px);
      box-shadow: 0 6px 15px rgba(233,158,185,0.3);
    }

    .mensagem-vazia {
      color: #700a32;
      font-size: 1.1rem;
      margin-top: 20px;
    }
  </style>
</head>
<body>
  <%- include('../partials/header') %>

  <main>
    <section class="pedidos-container">
      <h1>Meus Pedidos</h1>

      <% if (pedidos && pedidos.length > 0) { %>
        <div class="pedidos-grid">
          <% pedidos.forEach(pedido => { %>
            <div class="pedido-card">
              <div class="pedido-header">
                <h3>Pedido #<%= pedido.id %></h3>
                <span><%= new Date(pedido.data).toLocaleDateString() %></span>
              </div>

              <span class="pedido-status <%= pedido.status.replace(' ', '\\ ') %>">
                <% if (pedido.status === 'pendente') { %>Pendente
                <% } else if (pedido.status === 'em preparo') { %>Em preparo
                <% } else if (pedido.status === 'finalizado') { %>Finalizado
                <% } else if (pedido.status === 'a caminho') { %>A caminho
                <% } else if (pedido.status === 'entregue') { %>Entregue
                <% } else if (pedido.status === 'cancelado') { %>Cancelado <% } %>
              </span>

              <p class="pedido-total">Total: R$ <%= Number(pedido.total).toFixed(2) %></p>

              <a href="/loja/pedidos/<%= pedido.id %>" class="btn-detalhes">Ver detalhes</a>
            </div>
          <% }) %>
        </div>
      <% } else { %>
        <p class="mensagem-vazia">Você ainda não fez nenhum pedido!</p>
      <% } %>
    </section>
  </main>

  <%- include('../partials/footer') %>
</body>
</html>
