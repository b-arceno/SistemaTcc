<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Cadastro - TiaLuDoces</title>
  <link rel="stylesheet" href="/css/style.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
</head>
<body>

  <%- include('partials/navbar') %>

  <div class="top-bar">TiaLuDoces</div>

  <div class="cadastro-wrapper">
    <div class="cadastro-img">
      <img src="/imagens/doces.png" alt="Imagem ilustrativa" />
    </div>

    <div class="cadastro-form-card wide">
      <div class="tabs">
        <a href="/login" class="tab">Login</a>
        <span class="tab active">Registrar</span>
      </div>

      <% if (erro) { %>
        <p style="color: red; font-weight: bold;"><%= erro %></p>
      <% } %>

      <% if (sucesso) { %>
        <p style="color: green; font-weight: bold;"><%= sucesso %></p>
      <% } %>

      <form action="/registro" method="POST" class="formulario">
        <div class="input-icon">
          <i class="fa fa-user"></i>
          <input type="text" name="nome" placeholder="Nome" required />
        </div>
        <div class="input-icon">
          <i class="fa fa-envelope"></i>
          <input type="email" name="email" placeholder="E-mail" required />
        </div>
        <div class="input-icon">
          <i class="fa fa-phone"></i>
          <input type="tel" name="telefone" placeholder="Telefone" required />
        </div>
        <div class="input-icon">
          <i class="fa fa-lock"></i>
          <input type="password" name="senha" placeholder="Senha" required />
        </div>
        <button type="submit" class="btn">Cadastrar</button>
      </form>

      <a href="/login" class="msg-link">Já tem conta? Faça login</a>
    </div>
  </div>
</body>
</html>
