<%- include('../partials/adminHeader', { titulo: 'Pedidos' }) %>

<h1>Gerenciar Pedidos</h1>

<table class="tabela-admin">
  <thead>
    <tr>
      <th>ID</th>
      <th>Cliente</th>
      <th>Status</th>
      <th>Total</th>
      <th>Ações</th>
    </tr>
  </thead>
  <tbody>
    <% pedidos.forEach(p => { %>
      <tr>
        <td><%= p.id %></td>
        <td><%= p.cliente %></td>
        <td><%= p.status %></td>
        <td>R$ <%= p.total.toFixed(2) %></td>
        <td>
          <a href="/admin/pedidos/<%= p.id %>" class="btn">Ver</a>
        </td>
      </tr>
    <% }) %>
  </tbody>
</table>

<%- include('../partials/adminFooter') %>
